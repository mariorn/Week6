<div class="row">
  <div class="col s8 offset-s2">
    <h4>Details:</h4>

    <h5><%= "#{@product.title} - #{@product.description} - #{@product.deadline}" %></h5>
    <br><br>
    <%= render "products/form_reviews" %>
    <%= render "products/all_reviews" %>
    <%= link_to("New Bid", new_product_bid_path(@product), class: "waves-effect waves-light btn") %> 
    <br><br>
    <%= link_to("Actual Bids", product_bids_path(@product), class: "waves-effect waves-light btn") %> 
    <br><br>
    <%= link_to("Back", products_path(@user), class: "waves-effect waves-light btn") %> 
  </div>
</div>